import React from "react";
import { css } from "@emotion/react";
import styled from "@emotion/styled";

const NavigationChats = () => {
  const navChatStyle = css`
    height: 100%;
    width: 75px;
    background: #000000;
    opacity: 0.7;
  `;

  return <nav css={navChatStyle}>

  </nav>;
};

export default NavigationChats;
